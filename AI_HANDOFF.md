# 卞冰晶个人品牌网站：AI 接手说明

## 项目目标

这是卞冰晶的个人品牌与合作网站。网站不是把她包装成单一职业，而是展示她作为跨领域内容合作者的真实能力：内容策划与制作、老板 IP 起号与培训、纪录片与影视项目协作、短剧写作、AI 研究，以及泉州在地观察。

网站的主要访客是中小企业老板、品牌内容团队、文化项目和影视合作方。每次修改都要优先回答三个问题：访客能否快速知道她能帮助谁；能否看到真实的角色、过程与结果；能否很容易找到合作入口。

## 当前设计语言

正式首页采用“优雅未来感紫蓝 + 约 20% 田野元素”。深夜蓝紫、冷银、冰灰和少量电光蓝构成主色关系；田野元素只通过地点、观察编号、项目材料、现场语句和真实图片进入，不要增加乡土装饰、复古滤镜或高亮霓虹。

视觉上保持克制、专业、清晰和有辨识度。避免把卞冰晶写成电影导演；影视项目是能力证据，不是唯一职业标签。不要虚构客户、评论、评分、奖项、项目结果或影展信息。只有用户确认过的内容才能作为事实展示。

## 重要页面

`client/src/pages/Home.tsx` 是正式首页，包含首屏、影展与视频入口、关于我、代表项目、经历轨迹、兴趣研究和合作入口。

`client/src/pages/Reference.tsx` 是独立的视觉参考页，访问 `/reference`。它用于比较个人品牌表达方向，不应自动替代正式首页，除非用户明确要求。

`client/src/pages/ProjectDetail.tsx` 用于作品详情页。详情页应围绕“项目背景—我的角色—现场过程—结果证据—合作方式”展开，并优先使用真实项目照片。

`client/src/pages/reference.css` 是参考页专用样式。全局设计令牌和正式首页样式在 `client/src/index.css` 中维护。所有图片资源必须引用项目资源路径或稳定上传路径，不要把大文件直接塞进 `client/public`。

## 内容更新规则

新增项目时，请先整理项目名称、公开状态、背景、用户具体角色、关键判断、过程、结果证据、地点、时间、照片说明和外部链接。照片说明应包含地点或现场语境，但不要把无法确认的年份、影展、客户名称写成事实。

当用户只提供零散资料时，可以先形成待确认文案，并明确标注需要用户确认的字段。不要为了填满页面而制造案例、用户评价、客户 logo 或统计数字。

## 修改后必须验证

使用 `pnpm run check` 进行 TypeScript 检查，使用 `pnpm run build` 进行生产构建。至少检查 `/` 和 `/reference`，若修改作品详情，则同时检查 `/work/taikuixiang`、`/work/ip-training` 和 `/work/film-production`。需要同时关注桌面端与移动端，尤其是标题换行、图片裁切、CTA 对比度和导航可达性。

## 协作方式

其他 AI 可以直接阅读本文件、`README.md` 和 `todo.md` 后修改项目。建议每次只完成一个明确主题，例如“更新一个项目案例”“替换一组真实现场照片”或“调整首页合作入口”。修改后保留简短提交说明，并不要重置或覆盖其他会话已经完成的工作。

GitHub Pages 公开地址：<https://huadiao911.github.io/personal-brand-portfolio/>。
